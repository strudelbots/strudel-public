name: run strudel-test
on:
  workflow_dispatch:
  push:
    branches:
      - '**'
      - (!main)
jobs:
  run-strudel-test:
    uses: strudelbots/strudel-public/.github/workflows/run_file_change_test.yml@0.13.01
    with:
      # Make sure to change the name of your master branch if it is not main
      master_branch: main

